#facts is nothing but variable that is going to generated by ansible at the time of reconnect to the code
- name: Demo on facts
  hosts: localhost
  tasks:
    - name: Print Arch
      ansible.builtin.debug:
        msg: "{{ ansible_architecture }}"
    - name: Print Arch
      ansible.builtin.debug:
        msg: "{{ ansible_bios_version }}"